# Dependencies
node_modules/
__pycache__/
*.pyc

# Python
venv/
env/
.venv/
*.egg-info/
.pytest_cache/

# Node/Frontend
dist/
dist-ssr/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# package-lock.json # Optional: Some teams keep this, but if you want to ignore it for now (usually we KEEP it)
# usage: keep package-lock.json in git usually, so I won't ignore it.

# Environment variables (Secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db